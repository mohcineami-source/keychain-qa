# styles/

Global styling lives in `app/globals.css` (Tailwind layers + design-system helpers)
and the design tokens are defined in `tailwind.config.ts`. This directory is reserved
for any future standalone stylesheets (e.g. third-party CSS overrides). No additional
stylesheets are required for the current build.
